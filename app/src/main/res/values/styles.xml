<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="CustomAlertDialogStyle">
        <item name="android:bottomBright">@color/transparent</item>
        <item name="android:bottomDark">@color/transparent</item>
        <item name="android:bottomMedium">@color/transparent</item>
        <item name="android:centerBright">@color/transparent</item>
        <item name="android:centerDark">@color/transparent</item>
        <item name="android:centerMedium">@color/transparent</item>
        <item name="android:fullBright">@color/transparent</item>
        <item name="android:fullDark">@color/transparent</item>
        <item name="android:topBright">@color/transparent</item>
        <item name="android:topDark">@color/transparent</item>
    </style>

    <style name="LoadingDialogThemeWithOverlay" parent="android:Theme.Holo.Dialog">
        <item name="android:alertDialogStyle">@style/CustomAlertDialogStyle</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:textColorPrimary">@color/colorPrimary</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:textColor">@color/imgBlue</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="LoadingDialogThemeWithoutOverlay" parent="LoadingDialogThemeWithOverlay">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="InputTextBox" parent="Widget.Design.TextInputLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/scale_40dp</item>
        <!--<item name="android:layout_margin">@dimen/scale_20dp</item>-->
        <!--<item name="android:layout_marginTop">@dimen/scale_20dp</item>-->
        <item name="android:layout_marginTop">@dimen/scale_20dp</item>

        <!--<item name="android:layout_marginLeft">@dimen/scale_20dp</item>
        <item name="android:layout_marginRight">@dimen/scale_20dp</item>-->

        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/colorNormalGrey</item>
        <item name="android:textSize">@dimen/txt_16sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
        <item name="colorControlNormal">@color/colorNormalGrey</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <!--<item name="android:paddingBottom">@dimen/scale_10dp</item>
        <item name="android:layout_marginBottom">@dimen/scale_10dp</item>-->
    </style>
</resources>
